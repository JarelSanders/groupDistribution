import { GroupNamesComponent } from "../group-names/group-names.component";

export class Todo {
  // Declare a property 'User' of type string and initialize it with an empty string
  User: String = '';
  // Declare a property 'Group' of type 'any'
  Group: any ;
}
